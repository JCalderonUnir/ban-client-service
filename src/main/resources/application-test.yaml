spring:
  application:
    name: client-service

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

  flyway:
    enabled: true

logging:
  level:
    org.testcontainers: INFO
    org.springframework.test: INFO